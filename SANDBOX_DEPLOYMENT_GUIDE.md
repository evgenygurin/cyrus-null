# Cyrus Agent & Web Panel - Sandbox Deployment Guide

🎯 **Статус**: ✅ Успешно развернуто и готово к использованию

## Что было развернуто

### 1. Веб-панель мониторинга (✅ Активна)
- **URL**: http://localhost:3001
- **Статус**: Запущена в production режиме
- **Функции**: 
  - Реал-тайм дашборд с динамическими данными
  - Мониторинг активных сессий
  - Статистика производительности
  - Лента активности
  - Health check endpoint

### 2. Технические детали
- **Порт**: 3001 (адаптировано для Codegen sandbox)
- **Режим**: Production build
- **Обновления**: Каждые 3 секунды
- **Архитектура**: Next.js 15 + React 19 + TypeScript

## Особенности демо

### Динамические мок-данные
Панель показывает реалистичные данные, которые меняются каждые 3 секунды:

- **Статистика**: Общее количество сессий, активные сессии, процент успеха
- **Активные сессии**: Различные GitHub issues с динамическими статусами
- **Активности**: Имитация событий старта сессий, создания PR, завершения задач
- **Репозитории**: backend-api, frontend-app, mobile-app, docs-site, auth-service

### Примеры активностей
- "Analyzing code structure"
- "Writing unit tests"
- "Creating pull request"
- "Running tests"
- "Executing bash command"

## Как использовать

### Доступ к панели
```bash
# Основная панель
curl http://localhost:3001

# Health check
curl http://localhost:3001/api/health
```

### В браузере
Откройте в браузере: http://localhost:3001

### Что вы увидите
1. **Заголовок** с логотипом Cyrus и статусом "Agent Online"
2. **Статистические карточки** с ключевыми метриками
3. **Активные сессии** с деталями по каждой задаче
4. **Ленту активности** с последними событиями

## Для Codegen Sandbox

### Настройка Web Preview
В настройках репозитория Codegen:

```bash
Command: cd apps/web-panel && pnpm install && pnpm build && pnpm start
Port: 3001
```

Или используйте готовый скрипт:
```bash
./apps/web-panel/start-in-codegen.sh
```

### Ожидаемое поведение
1. Codegen автоматически обнаружит сервис на порту 3001
2. Появится кнопка "View Web Preview"
3. Откроется панель в новой вкладке браузера
4. Данные будут обновляться в реальном времени

## Архитектурные решения

### Изменения для Sandbox
- **Порт изменен с 3000 на 3001** (3000 занят nginx)
- **Production build** для быстрого запуска
- **Увеличена частота обновлений** до 3 секунд для демо

### Файловая структура
```
apps/web-panel/
├── src/
│   ├── components/dashboard/
│   │   ├── Dashboard.tsx      # Главный компонент (обновлен)
│   │   ├── StatsCard.tsx      # Статистические карточки
│   │   ├── SessionCard.tsx    # Карточки сессий
│   │   └── ActivityFeed.tsx   # Лента активности
│   └── app/
│       ├── page.tsx           # Главная страница
│       └── api/health/        # Health check endpoint
└── package.json               # Обновлен под порт 3001
```

## Следующие шаги

### Для полной функциональности
1. **Настроить Linear интеграцию** с реальными токенами
2. **Развернуть Cyrus CLI агент** для обработки issues
3. **Настроить API интеграцию** между агентом и панелью
4. **Подключить webhook от Linear** для реал-тайм обновлений

### Для разработки
1. Клонировать репозиторий
2. Установить зависимости: `pnpm install`
3. Запустить в dev режиме: `pnpm dev`
4. Настроить переменные окружения для API

## Поддержка

- **Документация**: См. файлы в `docs/` и `apps/web-panel/`
- **Issues**: Создавайте через GitHub Issues
- **Конфигурация**: Настройки в `apps/web-panel/src/`

---

**Дата развертывания**: 2025-01-03  
**Версия**: 0.1.0  
**Статус**: ✅ Готово к демонстрации